import { StyleSheet } from 'react-native';

export const title_style = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    color: 'red',
  },
});

export const food_group_card_style = StyleSheet.create({
  container: {},
});
